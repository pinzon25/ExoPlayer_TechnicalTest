<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ricard.projecte.exoplayer_technicaltest">

    <uses-permission android:name="android.permission.INTERNET" />
    <!--Afegir la linia de multidex permet evitar errors de compilacio en versions de sdk anteriors a la 20, la implementem degut a que fem servir la sdk 18. -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="androidx.multidex.MultiDexApplication"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.ExoPlayer_TechnicalTest">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        />

</manifest>